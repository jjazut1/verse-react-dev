/**
 * Game-related constants
 */

/**
 * Maximum number of items allowed per category in sort-categories-egg game
 */
export const MAX_ITEMS_PER_CATEGORY = 50;

/**
 * Minimum number of items required per category in sort-categories-egg game
 */
export const MIN_ITEMS_PER_CATEGORY = 3;

/**
 * Maximum number of categories allowed in sort-categories-egg game
 */
export const MAX_CATEGORIES = 10;

/**
 * Minimum number of categories required in sort-categories-egg game
 */
export const MIN_CATEGORIES = 2; 